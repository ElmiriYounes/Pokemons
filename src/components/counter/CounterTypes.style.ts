import styled from "@emotion/styled/macro";
import { Box } from "@mui/material";
import { flexCenter } from "src/globalStyle";

export const CounterUpWrapper = styled(Box)`
  padding: 20px;
  background-color: lavender;
`;
